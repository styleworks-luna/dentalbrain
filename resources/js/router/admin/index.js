import Vue from 'vue';
import Router from "vue-router";

// pages


Vue.use(Router);

const routes = [
];

const router = new Router({
    base: '/',
    mode: 'history',
    routes: routes
});

export default router;
